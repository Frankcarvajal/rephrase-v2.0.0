import React, { Component } from 'react';
import './output.css';

class Output extends Component {
    constructor(props) {
        super(props)
        this.toTranslate = "";
    }

    // Write a method that updates the empty string as the user speaks

    render() {
        return (
            <div>
            </div>
        );
    }
}

export default Output;